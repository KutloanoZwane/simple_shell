#include "shell.h"

/**
 * main - simple shell program that prints
 * and prompt the name of the shell.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *nameofShell = "KT_shell";

	prompt(nameofShell);
	return (0);
}
